services:
  app:
    build: .
    container_name: artbyo3
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - uploads_data:/app/uploads
      - works_data:/app/data
    env_file:
      - .env

volumes:
  uploads_data:
  works_data:
